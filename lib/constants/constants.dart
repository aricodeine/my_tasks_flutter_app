import 'package:flutter/material.dart';

const kAppTitleTextStyle = TextStyle(
  fontWeight: FontWeight.bold,
  fontSize: 28,
);

const kAlertDialogInputDecoration = InputDecoration(
  hintText: 'Enter a new task',
  border: OutlineInputBorder(),
);
